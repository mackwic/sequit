import { AI_DOCUMENTARY_EFFORT_SOURCE } from '../../../lib/document/example-document';
import type { PageLoad } from './$types';

export const load: PageLoad = () => ({ source: AI_DOCUMENTARY_EFFORT_SOURCE });
