[tools]
pnpm = "12.3.4"
node = "24.20.0"

[tasks.install]
description = "Install project dependencies"
run = "pnpm install --frozen-lockfile"

[tasks.dev]
description = "Run the local web and collaboration stack"
run = "pnpm dev"
